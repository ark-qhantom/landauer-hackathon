# real-run-4070/ (placeholder — filled by the RTX 4070 capture)

Run `../../4070-CAPTURE.md` on the 4070, then: `cp -R out/run-* submission-artifacts/real-run-4070/`
This lands the REAL nvidia-smi artifacts (energy_source=nvidia-smi, REAL measured Joules).
The video pipeline (video/make_frames.py) auto-detects this folder and renders Beats 4-6 with REAL data.
Empty by design until the capture.
